{
  "name": "cycle-count-app",
  "version": "1.0.0",
  "description": "Professional inventory cycle counting application with barcode scanning",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix ./backend\" \"npm run dev --prefix ./frontend\"",
    "build": "npm run build --prefix ./frontend",
    "start": "npm start --prefix ./backend"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
